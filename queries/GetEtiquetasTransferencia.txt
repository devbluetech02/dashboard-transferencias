SELECT
  TRANSACAO,
  CODBARRAS,
  FILIALORIG,
  FILIALDEST,
  DATA,
  CODFUNC,
  CONFIRMADO,
  LEUCODBARRAS,
  CODFUNCLEITURA,
  DATALEITURA,
  PERMITEINCLUIR,
  DATACONFENT,
  CODFUNCCONFENT,
  DATACONFSAI,
  CODFUNCCONFSAI,
  TRANSFESTOQUE,
  VERSAOROTINA,
  CODPROD,
  QUANTIDADE
FROM WINDOW.TAB_TRANSF_ETIQUETA
WHERE TRANSACAO >= 1442
  AND TRANSACAO = {{ Number(List1.selectedItem.TRANSACAO) }}
  AND CODPROD   = {{ Number(List2.selectedItem.CODPROD) }}
ORDER BY CODBARRAS
